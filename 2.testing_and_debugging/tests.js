QUnit.test( "hello test", function( assert ) {
    assert.ok( 1 == "1", "Passed!" );
    assert(false, "Fail!");
});

QUnit.test( "New test", function( assert ) {
    assert.ok( 1 == "1", "Passed!" );
    assert(false, "Fail!");
});